$schema: "https://json-schema.org/draft/2020-12/schema"
type: object
additionalProperties: false
required: [sample_id, dimension, score, hard_failures, rationale, confidence, evaluator_run_id]
properties:
  sample_id: {type: string, minLength: 1}
  dimension: {type: string, minLength: 1}
  score: {type: integer, minimum: 0, maximum: 10}
  hard_failures:
    type: array
    uniqueItems: true
    items:
      enum: [SEMANTIC_INVERSION, IDENTITY_CONFUSION, CRITICAL_OMISSION, FABRICATION, OFFENSIVE_MISTRANSLATION]
  rationale: {type: string, minLength: 1}
  confidence: {type: number, minimum: 0, maximum: 1}
  evaluator_run_id: {type: string, minLength: 1}
